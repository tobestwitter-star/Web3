// SPDX-License-Identifier: MIT
pragma solidity 0.8.20;
interface Vm { function deal(address,uint256) external; function prank(address) external; function expectRevert(string calldata) external; }
contract ProtocolFixturesTest { Vm constant vm=Vm(address(uint160(uint256(keccak256("hevm cheat code"))))); function testMultiStepStateMachineReproduction() public { StateMachineVault v=new StateMachineVault(); v.prepare(); v.execute(address(this),1 ether); require(v.credit(address(this))==1 ether,"security property violated"); } function testReentrancySequenceReproduction() public { ReentrancyBank b=new ReentrancyBank(); ReentrancyAttacker a=new ReentrancyAttacker(b); vm.deal(address(a),1 ether); vm.prank(address(a)); a.attack(); require(a.reentries()==1,"security property violated"); } function testUnreachableCandidateIsSafe() public { UnreachableCandidate c=new UnreachableCandidate(); bool ok; try c.suspicious(){ok=true;}catch{} require(!ok,"security property violated"); } function testInvariantPreservingCandidate() public { InvariantPreserving s=new InvariantPreserving(); s.mint(address(this),100); uint256 beforeTotal=s.total(); s.transfer(address(1),40); require(s.total()==beforeTotal,"security property violated"); } function testAuthorizationRequiresSpecificTransition() public { AuthorizationSequence a=new AuthorizationSequence(); vm.expectRevert("operator"); a.execute(10); a.queueOperator(address(this)); a.execute(10); require(a.credit(address(this))==10,"security property violated"); } function testEconomicStateDependentReproduction() public { EconomicPool p=new EconomicPool(1000,1000); uint256 before0=p.reserve0(); uint256 before1=p.reserve1(); uint256 out=p.swap(100); require(out>0,"security property violated"); require(p.reserve0()*p.reserve1()!=before0*before1,"security property violated"); }
}
contract StateMachineVault { enum Phase { Locked,Prepared,Settled } Phase public phase; mapping(address=>uint256) public credit; function prepare() external{phase=Phase.Prepared;} function execute(address b,uint256 a) external{require(phase==Phase.Prepared,"not prepared");credit[b]+=a;phase=Phase.Settled;} }
contract ReentrancyBank { mapping(address=>uint256) public balance; function deposit() external payable{balance[msg.sender]+=msg.value;} function withdraw() external{uint256 a=balance[msg.sender];require(a>0,"empty");(bool ok,)=msg.sender.call{value:a}("");require(ok,"send failed");balance[msg.sender]=0;} }
contract ReentrancyAttacker { ReentrancyBank public immutable bank;uint256 public reentries;constructor(ReentrancyBank b){bank=b;}receive()external payable{if(address(bank).balance>=1 ether&&reentries<1){reentries++;bank.withdraw();}}function attack()external{bank.deposit{value:1 ether}();bank.withdraw();} }
contract UnreachableCandidate { uint256 public state; function suspicious()external{require(state==type(uint256).max,"unreachable state");state=0;} }
contract InvariantPreserving { mapping(address=>uint256)public balance;uint256 public total;function mint(address to,uint256 a)external{balance[to]+=a;total+=a;}function transfer(address to,uint256 a)external{require(balance[msg.sender]>=a,"insufficient");balance[msg.sender]-=a;balance[to]+=a;} }
contract AuthorizationSequence { address public owner;address public pendingOperator;mapping(address=>uint256)public credit;constructor(){owner=msg.sender;}function queueOperator(address op)external{require(msg.sender==owner,"owner");pendingOperator=op;}function execute(uint256 a)external{require(msg.sender==pendingOperator,"operator");credit[msg.sender]+=a;pendingOperator=address(0);} }
contract EconomicPool { uint256 public reserve0;uint256 public reserve1;constructor(uint256 a,uint256 b){reserve0=a;reserve1=b;}function swap(uint256 a)external returns(uint256 o){o=(a*reserve1)/reserve0;require(o<reserve1,"too much");reserve1-=o;} }
