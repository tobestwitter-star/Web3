# Unseen holdout benchmark

These fixtures are intentionally separate from `benchmarks/fixtures/` and are not loaded by the analyzer. Labels live only in `holdout_manifest.json`, consumed by the holdout runner after analysis.

The corpus uses distinct names, structures, and control-flow patterns from the known regression suite. It is authorized only for isolated local analysis.
